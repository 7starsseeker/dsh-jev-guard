# 安全策略

> [English](SECURITY.md) | **简体中文**

## 这个项目是什么

`dsh-jev-guard` 是挂在 DSH 原生 `tools/pre-execute` 上的**事故安全网**,不是**安全边界**——它不防蓄意绕过。这是**显式决策**,见 [`docs/DECISIONS.md`](docs/DECISIONS.md) 的 **D1**,那里同时列出了有意接受的全部旁路。**报"绕过"之前请先读 D1。**

下面三条是**已知且有意保留**的,不是漏洞:

| 已接受的旁路 | 为什么不堵 |
|---|---|
| 模型用写文件的工具直接写授权文件 | 堵它等于把 `write` / `edit` 也拉进判定,或者做文件级权限——那是**另一个产品**,是沙箱的活 |
| 把命令改写成另一种写法以改变判定结果 | 这类问题的答案是**分层**,不是单点保证——见 **D2** |
| 提示注入让模型去做恶意行为 | 需要沙箱、低权限用户或容器——**显式**不在范围内 |

## 离开你机器的东西

判定服务收到的是**命令原文**,以及 `inlineScripts` 打开时**被调用脚本的正文**(命中 `.env` / `.ssh` / `*.pem` / `*credential*` / `*secret*` / `*token*` 的路径自动跳过,单文件上限 8 KB)。想要只发命令原文就设 `inlineScripts: false`——代价是 `node x.mjs` 这类调用退回 p≈0.31 的盲区。除此之外不传输任何东西:没有别的文件内容,没有会话历史,没有环境变量。

## 凭据

密钥按「DSH 凭据层 → 环境变量 → 你自己在包根建的 `secrets.json`」三级解析,并且**永不打印**;写进审计日志之前先做掩码。`node bin/guard.mjs key status` 报的是哪一级解析到了、值有多长,不是值本身。

## 失败一律放行(有意为之)

超时、网络错、5xx、429 都**放行**并记 `source: error`。阀门是**增量**检查:它之后 DSH 自己的沙箱档位仍然生效(`danger-full-access` 除外)。改成 fail-closed 会让一个"防事故"的东西变成"制造事故"的东西——见 **D3**。想要"服务挂了也拦",正确做法是把 L0 规则加厚。

## 审计日志

每条判定向 `~/.jev-guard/guard.log`(Windows 是 `%USERPROFILE%\.jev-guard\guard.log`)追加一行 JSONL,超过 4 MiB 轮转。命令原文在写入前已做掩码。每行记下判定结果、概率、这一条是规则给的还是模型给的、命中的规则 id,以及当时的沙箱档位与审批策略。不想留记录就把这个文件删掉。

## 报告漏洞

走 GitHub 的私密渠道:本仓库的 **Security** → **Report a vulnerability**。它会开一个只有维护者能看的私密 advisory——**在修复存在之前,能被人利用的问题请不要开公开 issue。**

一份有用的报告会说清:你跑了什么、你预期阀门怎么判、它实际怎么判,以及那一条的审计行(`node bin/guard.mjs log --tail 5`)。**不需要真的毁掉什么东西**:一条判错的命令原文就够了,它会进回归集。

## 范围

在范围内:判定层、L0 规则集与其锚定、令牌文件、审计日志的掩码与轮转、降级状态机、CLI。不在范围内:DSH 本体、它的沙箱档位与审批界面——那些属于上游,[deepseek-ai/deepseek-harness](https://github.com/deepseek-ai/deepseek-harness)。
